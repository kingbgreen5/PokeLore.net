Generated berry data lives in `generated/` and can be refreshed from PokeAPI with:

```sh
npm run generate:berries
```

Keep hand-authored corrections, notes, and extra guide data outside `generated/`.
The generated index is intended for berry guide filtering, while generated detail
files are intended for per-item or per-berry pages.
