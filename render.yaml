services:
  - type: web
    name: pokelore-net
    runtime: static
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /pokemon/*
        destination: /index.html
      - type: rewrite
        source: /*
        destination: /index.html
